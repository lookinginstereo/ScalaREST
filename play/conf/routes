# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /rest/cookie               controllers.Application.getCookies
GET     /rest/cookie/:id           controllers.Application.getCookie(id: Int)
POST    /rest/cookie               controllers.Application.addCookie
DELETE  /rest/cookie/:id           controllers.Application.removeCookie(id: Int)
